# Notas abiertas y pendientes

Preguntas sin responder y recordatorios para fases siguientes. Al resolver una, mover la respuesta al documento que corresponda (visión, arquitectura o decisiones) y tacharla acá.

## Sincronización con CodigoBase (mejoras de plataforma post-fork)

- [x] ~~**(2026-07-20) Traer mejoras de plataforma que CodigoBase sumó después del fork (ADR-09)**~~ **RESUELTO, primera vuelta**: como ambos repos (`CodigoBase/API` y `CodigoBase/Cliente`) son git normales, se pudo diffear con precisión desde el commit exacto del fork (`cfea9b2` en API, `3b52233` en Cliente) contra su HEAD actual y clasificar los ~80 commits nuevos en plataforma/candidato vs. Budget/no aplica. Se trajo: (1) **rxResource fases 2 y 3** (front) — AcgFotos se había quedado solo con la fase 1 del ADR-0003 de CodigoBase; se portaron `shared/util/lookup-resource.ts` (nuevo) y `shared/forms/edit-form-base.ts` (nuevo, la refactorización que separa la lógica genérica de `EditComponentBase` — venía "escondido" dentro de un commit catalogado como Budget aunque el archivo en sí es 100% genérico, ojo con esa trampa si se repite el análisis) + ~29 archivos sin diffs contra el punto de fork (copy-paste directo) + merge a mano de `tenant-edit.component.ts` y `layout/menu/menu-item.model.ts` (éstos sí tenían cambios propios de AcgFotos — se aplicó el patch de rxResource encima, sin pisarlos). De paso, `MENU_ICON_BY_CODE` (mapa hardcodeado de íconos) se eliminó del front porque el seed de menús de AcgFotos ya tiene `ImagenWeb` válido para todos los códigos (verificado). (2) **Fix de `Correo.cs`** (validación SMTP solo corre si `EmailEnabled=true`; login no-anónimo exige usuario+password completos en vez de inferirse de vacíos) + resource key `ErrorEmailCredentials` agregada en los 3 `.resx`. Verificado: frontend 454/454 + lint OK; backend build OK + integración 494/494 (un fallo puntual en la primera corrida, `TenantLicenseQuotaTests.Mantener_cupo_no_es_rechazado`, resultó ser un flake de aislamiento entre tests — pasa solo y en la segunda corrida completa). **Descartado a propósito**: todo DAP/CGV/ApprovalCycle/BPF/T-PC parent-child/`SqlDatasetGateway` (Budget, no aplica), limpieza cosmética de comentarios muertos en Core/Base (bajo valor), reorganización de `Docs/`/setup de CodigoBase (solo referencia).
- [x] ~~**(2026-07-20) Corrección: revisión commit-por-commit encontró 1 ítem que faltaba**~~ **RESUELTO**: al repasar los 33 commits de Cliente uno por uno se había clasificado mal `e3f80e9` ("fix(e2e): log-04 falla en suite por conteo fijo sobre LogInfo acumulativo") como "solo docs" — en realidad tocaba código (`e2e/specs/logs.spec.ts`). Portado: E2E-LOG-04 pasa de conteo fijo (2/1/2) a conteo relativo (`totalInicial` leído antes de filtrar), porque `LogInfo` es cross-tenant y acumula filas durante toda la corrida de la suite. Con esta corrección, los ~80 commits (50 API + 33 Cliente) quedaron los 100% revisados uno por uno.
- [ ] **(Próxima vuelta, si se retoma)** Diffear de nuevo desde los mismos puntos de fork (ya no hace falta repetir el análisis desde cero) para ver qué sumó CodigoBase después del 2026-07-20. Quedaron sin evaluar en detalle (bajo valor, no bloquean): la limpieza de comentarios muertos en ~18 archivos de `TechBI.Core`/`TechBI.Base.*`, y las guías de setup/CONTRIBUTING de CodigoBase (útiles para leer, no para copiar).

## Preguntas para el fotógrafo (papá) — RESPONDIDAS (2026-07-15, movidas a docs/00-visión § "Datos del negocio")

- [x] ~~**Tamaños de impresión reales**~~ Varios y cambiantes → ya cubierto: el catálogo es libre y por evento (ABM de Eventos). Idea anotada en Fase 4: copiar el catálogo del evento anterior al crear uno nuevo.
- [x] ~~**¿Vende paquetes/promos?**~~ Rara vez, pero útiles → **adelantados de Fase 4 a Fase 3** (tildado en docs/03).
- [x] ~~**Volumen típico**~~ ≈ 3 cursos × 30 alumnos, 4–5 fotos mínimo por persona + grupales → 500–800 fotos (~3–8 GB) por evento. Con R2 el storage es barato (~USD 0,05–0,12/mes por evento); si algún día molestara, ya existen `FechaExpiracion` + la limpieza automática de eventos cerrados (backlog Fase 4) y la política de retención (nota de Deploy).
- [x] ~~**¿Cómo imprime?**~~ Laboratorio externo; pide medida específica, sin nomenclatura → lista de impresión CSV/PDF + originales agrupados; validar proporciones al exportar.
- [x] ~~**¿Retoca las fotos antes de mostrar?**~~ Sí, Photoshop antes de subir → entran ya editadas; sin versionado (reemplazo = borrar y re-subir).
- [x] ~~**Flujo de entrega deseado**~~ Casa por casa o en el colegio (a veces entrega el colegio) → el pedido NO pide dirección; nombre + teléfono alcanzan. Idea de Alberto (2026-07-15): **entrega digital por email** como opción futura (pediría el correo en el pedido) — ligada a "Venta de foto digital" del backlog (Fase 4, anotado allá).
- [x] ~~**¿Cobra seña?**~~ A veces seña, a veces total por adelantado → el ítem de Mercado Pago (Fase 3) contempla pago total o parcial.
- [x] ~~**Tarjetita QR por alumno — proceso de reparto**~~ **DECIDIDO (2026-07-16)**: la distribución es **post-evento por WhatsApp** — imprimir tarjetas exigiría tener a la gente presente. Se envía a cada familia el link de canje (o la tarjeta como imagen). La tarjeta impresa queda como respaldo opcional. Deriva un ítem de producto: **botón "compartir por WhatsApp" por participante** en la pantalla de Tarjetas (link `wa.me` prearmado con la URL de canje) — calza con "Links de WhatsApp prearmados" de Fase 3; conviene adelantarlo al armar el canje en Fase 2.

## Técnicas — resolver en la fase indicada

- [x] ~~**(Fase 0)** Diseño del watermark — texto~~ **TEXTO Y OPACIDAD DEFINIDOS**: `COPIA NO AUTORIZADA — ACG Fotos` (2026-07-16, en `Fotos:TextoWatermark`) y **opacidad 50%** (pedido de Alberto 2026-07-16: "mitad transparente"; ahora configurable en `Fotos:OpacidadWatermark`, antes constante 0.35). **Densidad ajustada** (2026-07-16, feedback "deja muchos huecos — más invasiva pero sutil"): letra más chica (~1/20 del ancho, antes 1/12) y grilla más apretada (pasos 1.25×/2.2× del texto, antes 1.6×/5×) — más marcas, cada una más discreta. Queda: decidir si algún día se suma logo.
- [x] ~~**(Fase 2)** **Segunda marca de agua: overlay en el front con el nombre del que mira**~~ **RESUELTO (2026-07-18)**: implementado en `tbi-foto-familia-img` (`foto-familia-img.component.ts`), así que aplica solo con tocar un componente tanto en la grilla como en el preview ampliado. El texto ("Familia de {participante(s)}") sale de `FamiliaSessionStore.participantes()` (la sesión canjeada), no de la foto — por eso cubre también las grupales, que no tienen `participanteId`. Se pinta como `background-image` con un SVG chico (150×70px, rotado -18°) repetido vía `background-repeat`, no como nodos DOM por tile, para no pegarle a la performance en gama baja con muchas miniaturas en pantalla. Tests: 2 casos nuevos (con sesión aparece con el nombre correcto; sin sesión no aparece) + suite completa 396/396 + lint. Límite conocido sigue en pie: no hornea el píxel — el watermark horneado de la capa 1 (backend) es el que nunca se va. Complemento opcional (hornear "Familia {nombre}" en el derivado de individuales) sigue evaluándose, no implementado.
- [x] ~~**(Fase 0)** Formato de previews JPEG vs WebP~~ **RESUELTO (2026-07-15)**: los derivados salen en **WebP** (25–30% menos peso, soporte >97% de navegadores) y con **resolución/calidad bien bajas a pedido de Alberto** — preview 900px / calidad 55, configurable en `Fotos:LadoMayorPreview|LadoMayorThumb|CalidadDerivados|OpacidadWatermark` para ajustar mirando fotos reales. Los originales no se tocan. **Referencia real del piloto (2026-07-16)**: original 7296×5472 px / 10,5 MB → preview 900×675 px / 40 KB (~1,5% de los píxeles, 260× menos peso; impreso daría ~150 dpi con watermark — inservible). **Cómo reprocesar tras cambiar config**: `UPDATE fot_Fotos SET EstadoProcesamiento = 0 WHERE ...` + reiniciar la API (el worker barre pendientes al arrancar); no hace falta re-subir. Ojo: las fotos ya procesadas en dev conservan derivados `.jpg` viejos — re-subirlas o borrar y volver a cargar.
- [ ] **(Fase 0)** Node global de la máquina es 22.14 y el shell Angular exige ≥22.22.3: por ahora se usa el Node portátil de `.tools/` — actualizar el Node global cuando sea cómodo y borrar `.tools/`.
- [ ] **(Fase 1)** Alta real del negocio: crear el tenant del fotógrafo y su usuario NO-root (el guard multi-tenant impide que root cargue datos de negocio en su propio tenant — está bien así: root administra, el fotógrafo opera). El ABM de tenants/usuarios heredado ya lo permite desde el front. **Ensayo en dev hecho**: `backend/scripts/dev-alta-fotografo.sql` crea el usuario `fotografo` (tenant 2, clave dev) con rol/permiso/licencia/app — verificado end-to-end (login, menús, alta de evento por API). Queda el alta REAL (tenant y usuario definitivos, desde los ABMs). **Bug corregido (2026-07-15)**: ningún rol tenía `EsDefaultParaNuevoTenant=1` (dato que el fork perdió del seed de CodigoBase) → los tenants creados por ABM quedaban "sin administradores" y su usuario admin sin rol ni menús. Ahora `Fotógrafo` es el rol default (habilitado para la licencia `adminCliente`) y `dev-alta-fotografo.sql` repara los tenants ya creados — ver Fase 0b en docs/03.
- [x] ~~**(Fase 1)** Revisar qué features de plataforma esconder del menú~~ **RESUELTO (2026-07-15)**: permiso `PermisoFotos` + rol **Fotógrafo** que solo lo tiene; los menús del vertical se re-apuntaron a ese permiso y el usuario `fotografo` pasó al rol nuevo (todo en `dev-alta-fotografo.sql`, aplicado a la base dev y verificado por API: el fotógrafo ve SOLO Fotos → Eventos/Grupos/Galería/Tarjetas). **Efecto colateral por diseño de la plataforma**: root arma su menú únicamente con PermisoRoot (hardcodeado en `MenuAppService`), así que root ya NO ve la sección Fotos — coherente con "root administra, el fotógrafo opera" (root asiste impersonando al fotógrafo). Si molestara en dev, la alternativa es tocar `MenuAppService` para unir PermisoRoot + permisos de roles.
- [ ] **(Fase 1)** Upload masivo desde el navegador: DECIDIDO empezar por la API (permite generar derivados ahí mismo), con procesamiento en background (`Pendiente → Lista`, ya modelado) para no bloquear el request. **Implementado** (`POST api/fotos/fotos/upload` + `FotoProcesamientoWorker`; límite 512 MB por request — el front sube por tandas). Queda: medir con un evento real; el escape si molesta es subida directa al bucket con URL firmada de escritura. **Regla de performance**: la API nunca proxya bytes de imagen hacia las familias en prod — solo firma URLs; el navegador baja del bucket/CDN.
- [ ] **(Fase 1)** Auto-asignación de fotos a participantes: el fotógrafo saca por orden de lista o por carpetas por persona. Si nombra carpetas por participante, el upload puede mapear carpeta → participante automáticamente. Preguntarle cómo organiza hoy.
- [x] **(Fase 1 — pedido del front al back)** ~~Seed de menú del vertical Fotos~~ **RESUELTO**: `TestSeed.sql` ahora siembra el árbol de menús completo (Fotos → Eventos/Cursos + Administración + Plataforma, RoutePaths calzados con `app.routes.ts`) y las mismas filas quedaron aplicadas a la base dev `AcgFotos`. Todos con `PermisoId=1` (PermisoRoot): root los ve; cuando se haga el alta real del fotógrafo no-root, su rol debe recibir ese permiso o re-apuntarse los menús a un permiso propio (ver nota de alta real).
- [ ] **(Deuda menor)** El host de tests de integración usa el MISMO `App_Data/storage` que la API dev (content root del proyecto Api): cada corrida deja archivos huérfanos de fotos/tenants mezclados con los datos reales de dev. Apuntar el storage del host de tests a un directorio temporal propio (override de `IWebHostEnvironment.ContentRootPath` o del provider en `TestWebApplicationFactory`). Mientras tanto, limpieza manual comparando `fot_Fotos.StorageKey` contra el disco (hecha 2026-07-13).
- [x] ~~**(Fase 2)** Rate-limiting y lockout en el canje de códigos (fuerza bruta)~~ **RESUELTO (2026-07-16)**: `POST api/fotos/canje` va con `[EnableRateLimiting(RateLimitingHelper.CanjePolicy)]`, política por IP igual que `LoginPolicy` (`RateLimiting:Canje:PermitLimit|WindowSeconds`, default 10/60s). Falta el lockout (bloqueo persistente tras N intentos, más allá de la ventana) — no implementado, evaluar si hace falta con uso real.
- [x] ~~**(Fase 2)** **Vigencia del QR/código**~~ **RESUELTO (2026-07-16)**: el canje (`CanjeAppService`) rechaza códigos con `Activo=false` y eventos que no estén `Publicado` o cuya `FechaExpiracion` ya pasó (mismo mensaje genérico para no revelar cuál caso fue). El token de sesión dura **30 minutos** (`Fotos:DuracionSesionFamiliaMinutos`, decisión de Alberto 2026-07-16): al vencer, re-escanear el QR o reingresar el código vuelve a abrir sesión mientras el evento siga vigente. Pendiente: el aviso "disponible hasta el X" y el cierre automático (Fase 3, sin cambios).
- [ ] **(Fase 2 — diseño del canje/sesión)** **Sesión multi-participante** (conversado 2026-07-15). Hoy: 1 código = 1 participante, y las grupales del grupo ya entran con ese mismo código (NO hace falta un segundo QR para "grupales" — no son un álbum aparte). Los casos que sí dejan a una familia con dos QR: hermanos en el mismo evento, o una persona en dos grupos (eventos genéricos: dos categorías de un torneo, dos talleres). Decisión propuesta (pendiente de confirmar con Alberto): NO tocar el modelo; diseñar el **token de sesión de Fase 2 con una LISTA de participanteIds** desde el día uno, y en la galería de familias ofrecer "agregar otro código" para sumar participantes a la misma sesión (un solo carrito/pedido). Alternativas descartables por ahora: código n─n participantes en el modelo (más admin, solo si el caso hermanos resulta frecuente) y foto n─n participantes (etiquetar una misma foto en varios álbumes — anotado en backlog Fase 4).
- [x] ~~**(Fase 2 — UX de entrada, pedido de Alberto 2026-07-18)** **Botón "Entrar con código" en el login de la plataforma**~~ **RESUELTO (2026-07-18)**: link "Entrar con código" agregado en `login.component.html` (bajo el botón "Ingresar"), con `routerLink="/canje"` — reutiliza el login heredado de la plataforma en vez de una landing propia; no requirió tocar el guard ni la sesión (es solo navegación, no hay dato sensible de por medio). Test nuevo en `login.component.spec.ts` verifica el `href`.
- [x] ~~**(Fase 2 — Galería, dispositivo compartido)** **Botón "Cerrar sesión" / "Salir" en la zona de familias**~~ **RESUELTO (2026-07-21, commit `f5eed77`)**: botón en el header de `mi-album` (`MiAlbumComponent.cerrarSesion()`) que llama a `FamiliaSessionStore.clearSession()` y navega a `/canje`. No hizo falta un `CarritoStore.vaciar()` explícito: el carrito ya se vacía solo al cambiar de sesión de familia (mismo criterio de dispositivo compartido documentado en el carrito). Alcance reducido respecto a la idea original: solo en `mi-album` (no en `carrito`) y sin diálogo de confirmación previo.
- [x] ~~**(Fase 2 — Galería)** **Bug reportado 2026-07-19: la flecha del carrusel (preview ampliado de `/mi-album`) no funciona.**~~ **RESUELTO (2026-07-21)**: verificado manualmente por Alberto, ya no se reproduce. No se identificó un commit puntual que lo haya corregido (posible efecto colateral de otro cambio sobre `foto-familia-preview-dialog.component.ts`/`FotoFamiliaImgComponent` en el medio) — si reaparece, retomar desde los candidatos ya anotados en su momento (z-index del `.nav`, manejo de `fotoId` cambiante en `rxResource`).
- [x] ~~**(Fase 2 — Galería)** **Info de la foto en el carrusel**~~ **RESUELTO (2026-07-18, junto con el Carrito)**: el "checklist de selección" quedó resuelto de la forma más directa — `tbi-agregar-carrito` vive inline en el preview ampliado (no solo un dato pasivo) y su botón cambia a "Agregar más (N en el carrito)" cuando esa foto+tamaño ya está en el carrito, así se ve y se edita sin volver a la grilla. No se agregó otro dato de la foto (nombre del participante ya se ve en la grilla en vista lista/chip "Grupal"; no pareció necesario duplicarlo en el preview).
- [x] ~~**(Fase 2)** EXIF: limpiar metadatos (GPS, equipo) de los derivados antes de servirlos~~ **RESUELTO (2026-07-18)**: `ImageSharpImageProcessor.LimpiarMetadatos` (`AcgFotos.Fotos.Infrastructure/Imaging/ImageSharpImageProcessor.cs`) pone en `null` `ExifProfile`/`IptcProfile`/`XmpProfile` del derivado clonado ANTES de aplicar el watermark y guardarlo — el original nunca se toca (sigue con su EXIF intacto, útil para el fotógrafo/laboratorio). Se conserva el `IccProfile` (perfil de color, no es dato personal). Test de regresión (`ImageProcessorTests.Limpia_el_EXIF_del_original_GPS_y_equipo_en_ambos_derivados`): genera un JPEG con `ExifTag.Model` y `ExifTag.GPSLatitudeRef` seteados y verifica que preview y thumb salen sin `ExifProfile`.
- [x] ~~**(Fase 2 — hallazgo de seguridad, 2026-07-18)** `AuthorizationEnabled=false` deja sin efecto TODO chequeo por endpoint~~ **DEFENSA EN PROFUNDIDAD APLICADA (2026-07-18)**: se descartó prender `AuthorizationEnabled=true` globalmente — es un flag de PLATAFORMA (no de Fotos): `TestSeed.sql` no siembra ningún permiso→endpoint/rol→permiso por defecto, y decenas de tests no-Fotos (Auditoría, Catálogos, Tenants...) asumen el host base con authz off a propósito; prenderlo hoy exige diseñar y sembrar el catálogo de permisos de TODA la plataforma heredada, un trabajo aparte y mucho mayor (decisión de Alberto 2026-07-18: "defensa puntual" en vez de ese rollout completo). En su lugar: `FamiliaSessionGuard.EnsureNoFamiliaSession` (`AcgFotos.Fotos.Application/Security`) — llamado al inicio de TODOS los métodos públicos de `EventoAppService`, `GrupoAppService` y `FotoAppService` — rechaza con 403 (`ForbiddenException`, nueva en Core, mapeada en `ExceptionHandlingMiddleware`) cualquier sesión de familia, **sin depender de `AuthorizationEnabled`**. Cubre upload/listar/thumb/preview/original/borrar de fotos, CRUD de eventos y grupos, y tarjetas (códigos+QR — la fuga más grave si quedaba abierta). 4 tests de regresión (`FamiliaSessionAdminGuardTests`, corren a propósito en el host CON authz off) reproducen el hallazgo original (borrado de foto ajena) y confirman que ahora da 403; suite completa 487/487. El flag `AuthorizationEnabled` en sí sigue en `false` — la decisión de si/cuándo poblar el catálogo completo de permisos de plataforma queda pendiente y por fuera de Fase 2 (anotado como posible ítem de Deploy).
- [x] ~~**(Fase 2 — Carrito, UX, feedback de Alberto 2026-07-18)** Agregar varios tamaños de la misma foto en un solo paso~~ **RESUELTO (2026-07-19)**: ver el ítem de `tbi-agregar-carrito` más abajo (UX de compra) — quedó una fila por tamaño, cada una con su propio +/-.
- [ ] **(Fase 2 — UX de compra, revisión 2026-07-18/19 del prototipo `docs/ClaudeDesign/PropuestaDisenioExperienciaCompra`)** Alberto pidió comparar el prototipo hifi (galería/visor/carrito/checkout) contra `features/familia` real y dejar los gaps anotados para ir por partes; **por ahora foco en usabilidad de UI, la lógica de negocio queda para después**. Ya implementado/mejorado respecto al prototipo: grilla con densidad configurable (2/4/lista, el prototipo no la tenía), preview con carrusel + teclado, agregar al carrito inline con +/- directo (sin botón "Agregar" a confirmar). De los gaps de **UI/usabilidad**, 3 de 4 resueltos el 2026-07-19 (el filtro por álbum sigue pendiente, necesita backend — ver más abajo):
  - [x] ~~Selección múltiple en la grilla~~ **RESUELTO en 3 vueltas (2026-07-19)**: toggle "Seleccionar varias" en `MiAlbumComponent` — tilda fotos (checkmark circular por tile) sin abrir el preview mientras el modo está activo, con un `tbi-select` ("Tamaño") en la barra que se puede cambiar ANTES de tocar "Agregar seleccionadas". Dos bugs de la 2ª vuelta corregidos en la 3ª: (a) el resaltado del tildado directamente no se veía — causa real: `.marca-familia` (el watermark tileado dentro de `tbi-foto-familia-img`) tiene `z-index:1`, y ni `.grilla__tile` ni sus ancestros arman stacking context propio (ninguno tiene z-index) → ese `z-index:1` queda "flotando" y le gana a cualquier `outline`/`::after`/`border` sin z-index propio de `.grilla__tile`, sin importar el orden en el DOM. Fix real (4ª vuelta): `z-index` explícito en `.grilla__tile--seleccionada` (bajo, 1, para no taparle a `.grilla__check` que es 2) — así el tile arma SU PROPIO stacking context y encierra la fuga; + como refuerzo a prueba de fallos que NO depende de z-index en absoluto, las fotos NO tildadas se atenúan (`opacity:0.4`) mientras dura el modo selección. (b) El toast de "Agregar seleccionadas" seguía diciendo "podés ajustar el tamaño después" — mensaje viejo de cuando el tamaño era un default fijo; ahora que se elige ANTES de agregar, el mensaje pasó a listar lo que realmente pasó (`"N fotos agregadas en {tamaño} (1 copia c/u). Podés sumar más copias desde el carrito"`).
  - [x] **(4ª vuelta, 2026-07-19 — el punto 5 real, distinto al de arriba)** Alberto en realidad pedía algo más simple y separado del modo selección: un tilde PERSISTENTE en la grilla normal (no depende de "Seleccionar varias") que muestre qué fotos YA tienen copias en el carrito — calcado del `ph.inCart` del prototipo `docs/ClaudeDesign` (badge verde esquina superior-izquierda). **RESUELTO**: nuevo `.grilla__en-carrito` (esquina opuesta al botón de agregar, `@if (carrito.cantidadTotalDeFoto(foto.id) > 0)`), siempre visible sin importar el modo. De paso, pidió que el botón "agregar al carrito" de cada tile deje de estar siempre puesto (molestaba visualmente) — pasó a aparecer solo con `:hover`/`:focus-within` sobre el tile.
  - [x] ~~FAB/indicador de carrito con el total en pesos a la vista~~ **RESUELTO en 3 vueltas (2026-07-19)**: 1ª vuelta, pill ancho con cantidad + total — Alberto dudó ("puede intimidar"). 2ª vuelta, redujo a un círculo solo-ícono en la esquina (estilo Mercado Libre/Amazon, sin monto) — **pero quedó "muy escondido"** (mismo feedback, 3ª vuelta). Resuelto como punto medio: volvió a ser una píldora ancha centrada abajo, bien visible (ícono + "Ver carrito" + badge de cantidad), pero sin el monto — el precio sigue viéndose recién dentro de `/carrito`.
  - [x] ~~También falta el nombre de la foto~~ **RESUELTO (2026-07-19)**: `FotoFamilia` ganó `nombreArchivoOriginal` (el backend ya lo exponía en `FotoDto`, solo faltaba en el modelo del front) — overlay monospace en la esquina de cada miniatura de la grilla, línea secundaria en la vista lista, y label en el preview ampliado (esquina inferior derecha, junto con el "nombre legal" en la inferior izquierda — ver ítem del preview) y en cada línea del carrito.
  - [x] **(extra, feedback de Alberto 2026-07-19)** Preview ampliado — layout de las 4 esquinas: pidió replicar el del prototipo (`docs/ClaudeDesign`), que además dejaba afuera el "nombre legal". **RESUELTO**: cerrar (cruz) arriba-derecha, paginación "x / y" arriba-izquierda, nombre legal ("Familia de {participante(s)}", nuevo `FamiliaSessionStore.nombreFamilia`, compartido con el watermark tileado de `tbi-foto-familia-img` para no duplicar el `join`) abajo-izquierda, nombre de archivo abajo-derecha — las 4 superpuestas a la imagen, se eliminó `mat-dialog-actions` (la franja inferior ahora es solo el selector de tamaño+cantidad, mucho más chica).
  - [x] **(extra, feedback de Alberto 2026-07-19)** Densidad de grilla: pidió poder verla "aún más chica, las que entren de acuerdo a la pantalla". **RESUELTO**: nuevo botón "Muy chico" (`densidad: 'compacta'`) con `grid-template-columns: repeat(auto-fill, minmax(72px, 1fr))` — a diferencia de 2/4 (columnas fijas), la cantidad de columnas la calcula el navegador según el ancho real de pantalla; en esta densidad se ocultan el chip "Grupal" y el overlay de nombre de archivo (ilegibles a ese tamaño).
  - [x] **`tbi-agregar-carrito` (selector de tamaño+cantidad, usado en la grilla y el preview) — 3 vueltas.** 1ª: toggle-group horizontal (tamaños lado a lado) + un stepper aparte — el layout separaba tamaño y cantidad en extremos opuestos ("ir de un lado de la pantalla al otro"). 2ª: una fila por tamaño, cada una con su +/- — Alberto la rechazó: con varios tamaños en el catálogo es inviable, demasiadas filas en el preview ampliado. **RESUELTO (3ª vuelta)**: `tbi-select` (desplegable, el mismo componente que el selector de tamaño del lote) + UN solo stepper, agrupados con poco espacio entre sí (no `justify-content:space-between`) — el tamaño arranca preseleccionado en el primero del catálogo (default) y la familia lo cambia manualmente del desplegable solo si quiere otro. Esto también cierra el ítem viejo "Agregar varios tamaños de la misma foto en un solo paso" (se puede: cambiás el desplegable y sumás del otro tamaño, cada combinación foto+tamaño es su propia línea de carrito).
  - [x] **(5ª vuelta, 2026-07-19)** Tres ajustes más: (a) el panel de `tbi-agregar-carrito` en el preview ampliado — 1er intento lo mandó al costado de la imagen en pantallas anchas (mal interpretado); Alberto aclaró que quería seguir ABAJO (bottom-sheet, en toda resolución) pero con el contenido pegado a la derecha en vez de a la izquierda — **RESUELTO 6ª vuelta**: se sacó el `@media`/`flex-direction:row`, `.agregar` quedó `display:flex; justify-content:flex-end`; (b) el preview ampliado también muestra el tilde "ya en el carrito" (apilado bajo el contador, misma esquina — no hay una 5ª esquina libre) y `tbi-agregar-carrito` suma un resumen "Ya en tu carrito" con un chip sutil por tamaño (fondo `color-mix` con el primary al 12%, nada llamativo — pedido explícito de separar visualmente "10x15 ×2" de "20x30 ×1"; antes era un string plano unido con "·") — de solo lectura, cruza el mismo `CarritoStore`, no duplica estado; (c) la barra de "Seleccionar varias" repetía la palabra "seleccionadas" (un `<span>` aparte + el botón "Agregar seleccionadas") — se sacó el span y el contador quedó solo en el botón ("Agregar N fotos").
  - [x] **(7ª vuelta, 2026-07-19)** Dentro de `tbi-agregar-carrito`: el resumen "Ya en tu carrito" y el selector de tamaño+cantidad vivían en bloques separados, cada uno en su propia línea — Alberto pidió que compartan una sola fila (texto al lado del botón) siempre que el ancho alcance, Y que el selector no se corra de posición según haya o no resumen. **RESUELTO**: `.agregar-carrito__fila` pasó de flex a **grid de 3 columnas** (`1fr auto 1fr`) — el resumen va en la columna 1 (izquierda), el selector en la columna 2 (centro, `justify-self:center`), la columna 3 queda de contrapeso vacío. Con flex + `space-between` el selector "saltaba" de la izquierda (sin resumen, un solo hijo) a la derecha (con resumen, dos hijos); con grid la columna del medio es siempre la misma posición, tenga o no contenido la columna 1.
  - Frontend: 446/446 tests + lint + build verdes tras las 7 vueltas de feedback.
  - [x] ~~**(Para el próximo chat — pedido explícito de Alberto 2026-07-19)** UX de "post-selección"~~ **EN CURSO, primera vuelta RESUELTA (2026-07-19)**: filtro "ver solo las seleccionadas" en la grilla, ver ítem de arriba. Del pedido de que **`/carrito` siga las ideas del prototipo `docs/ClaudeDesign`**, se acotó explícitamente el alcance a lo puramente visual (charlado con Alberto antes de tocar código): **RESUELTO, con una vuelta de corrección de diseño el mismo día** — primer intento: líneas como card con chips de tamaño clicables para "reasignar" el tamaño de la línea ahí mismo. Alberto lo cuestionó con razón: si la misma foto ya tenía dos tamaños en el carrito, aparecían dos cards iguales y tocar un chip fusionaba una en la otra sin que fuera obvio — mala lectura del prototipo. **Diseño corregido**: se agrupan las líneas por foto (una sola card por foto, `CarritoComponent.grupos`) con una fila por tamaño (cantidad +/-, subtotal, quitar esa línea puntual) — mismo criterio que ya usa el chip de solo-lectura "Ya en tu carrito" del selector `tbi-agregar-carrito`. Se sacó el tamaño clicable y `CarritoStore.cambiarTamano` (quedaba sin uso). En su lugar, tocar la cabecera de la card (thumbnail+nombre) abre el mismo preview ampliado que la grilla (`FotoFamiliaPreviewDialogComponent`, navegando entre las fotos del carrito) — ahí sí está el selector completo de tamaño+cantidad para agregar otro tamaño, sin duplicar esa capacidad dentro del carrito. Card de resumen con el Total con más jerarquía. Sin banner de descuento por cantidad, sin upsell de pack digital, sin "Subtotal" separado — esos siguen pisando lógica de negocio no decidida (ver ítem de arriba, "UX de compra"). 453/453 tests + lint + verificación manual con Playwright (agrupamiento, preview desde el carrito). **3ª vuelta el mismo día (feedback de Alberto sobre el resultado)**: (a) el tipo/tamaño ("10x15") ahora se ve como chip con fondo de color en TODA la app (mismo tratamiento que el chip de solo-lectura "Ya en tu carrito"), no solo texto plano en el carrito — "debe colorearse solo el tipo", nada más de la línea lleva color; (b) `.carrito` pasa a `max-width: 560px; margin: 0 auto`, corrige el espacio absurdo entre tamaño y precio en desktop (la fila se estiraba a lo ancho de toda la pantalla) — mismo criterio "centrado" del prototipo `docs/ClaudeDesign`, y `.carrito__tamano-linea` pasa de `flex:1` en el nombre a `justify-content:space-between`; (c) la cabecera de la card (thumb+nombre) deja de ser un único botón que envolvía todo el bloque (quedaba "feo" con el hover cubriendo ambos) — ahora son dos botones independientes, cualquiera de los dos abre el mismo preview. 454/454 tests + lint + verificación visual mobile/desktop.
  - [x] ~~Pantalla de confirmación de pedido como vista propia~~ **RESUELTO**: nueva ruta `/pedido-confirmado` (`PedidoConfirmadoComponent`, guardada por `familiaSessionGuard`); `CarritoComponent.confirmar()` vacía el carrito y navega pasando el pedido por `NavigationExtras.state` (con fallback a `history.state`, sobrevive un F5). Sin pedido en el state (entrada directa a la URL) redirige a `/mi-album` en vez de mostrar un error — no es un caso de fallo real.
  - [x] ~~Filtro "ver solo las seleccionadas" en la grilla~~ **RESUELTO (2026-07-19)**: toggle "Ver solo el carrito" en `MiAlbumComponent`, hermano de "Seleccionar varias" (solo visible con `carrito.totalItems() > 0`) — filtra la grilla a las fotos con copias en el carrito (`fotosFiltradas`, `Set` de `fotoId` derivado de `carrito.lineas()`) para repasar lo elegido sin ir y volver de `/carrito`. El preview ampliado también respeta el filtro: con el toggle activo, `verPreview` arma el carrusel sobre `fotosFiltradas()` en vez de `fotos()`, así las flechas navegan solo entre lo ya agregado. Mensaje vacío propio ("No tenés fotos en el carrito todavía") si el filtro deja la grilla en cero. 3 tests nuevos + verificación manual con Playwright contra la API/front dev reales (evento `E2E Carrito Manual`). Sigue pendiente, sin decidir todavía: si conviene rehacer `/carrito` completo siguiendo `docs/ClaudeDesign` (banner de descuento, upsell, chips editables) — ver el ítem de arriba, "UX de compra", que pisa lógica de negocio no resuelta.
  - [ ] Filtro por álbum/sección (chips tipo "Ceremonia/Fiesta/Egresados") — **sin implementar, requiere backend**: hoy no existe ningún concepto de álbum/sección de foto en el dominio (`FotoFamilia` no tiene ese campo); se solapa con la idea de Fase 4 "Secciones por foto" (docs/03), no es un cambio solo de front.
  - [ ] **(Idea futura, 2026-07-19)** Densidad de grilla responsive de verdad: hoy "Grande"/"Chico" fijan una CANTIDAD de columnas (`repeat(2, 1fr)` / `repeat(4, 1fr)`), así que en pantallas angostas dan fotos desproporcionadamente grandes y en pantallas anchas no aprovechan el espacio extra para mostrar más fotos — solo "Muy chico" es responsive porque ya usa `auto-fill, minmax(...)`. Mejora propuesta: llevar los 4 niveles al mismo patrón (`auto-fill` con un ancho mínimo distinto por nivel, p.ej. grande ~220px / chico ~140px / muy chico 72px) para que el navegador calcule la cantidad de columnas según el ancho real. Implica retocar `aria-label`/tooltips ("2 por fila" deja de ser literal) y los 2 tests de `mi-album.component.spec.ts` que hoy verifican `repeat(2, 1fr)` exacto.
  - Gaps que dependen de **lógica/decisiones de negocio** (quedan para después, no tocar sin hablarlo): tipo de producto Impresión/Digital (`TamanoPrecio` hoy es homogéneo, sin descarga digital), descuento por cantidad (tiers 10%/20%), upsells (siguiente tamaño, pack digital), checkout como paso separado con método de pago (se solapa con Fase 3 — Mercado Pago/transferencia). La sección "Entrega a domicilio" del prototipo quedó obsoleta: ya está decidido que el pedido no pide dirección (docs/00-visión § Datos del negocio). "Modo fotógrafo" y el ABM de marca de agua configurable ya estaban anotados aparte, más abajo en este documento.
  - Nota de UI menor detectada al implementar: el build de producción tira un warning (no error) de presupuesto de estilos por componente en `mi-album.component.scss` (4,77 KB contra un warning a los 4 KB; el error recién sería a los 8 KB) — cosmético, no bloquea, evaluar si conviene trimmear estilos más adelante.
- [ ] **(Fase 2, follow-up de la lista de impresión, ADR-13)** **Ancho/alto numéricos en el catálogo de tamaños**: el aviso de desajuste de proporción de la lista de impresión (`detectarDesajusteProporcion`, frontend) hoy parsea `TamanoPrecio.Nombre` con regex (solo funciona con nombres tipo "10x15"; "Panorámica" o similares no matchean y no avisan). Si en uso real esto resulta insuficiente (muchos tamaños con nombres no numéricos, o el fotógrafo pide más precisión), agregar `Ancho`/`Alto` numéricos opcionales a `TamanoPrecio` (migración EF + campos en el ABM de Eventos) y usarlos en vez del parseo — ameritaría su propio ADR (ver alternativa descartada en ADR-13).
- [ ] **(Fase 2/3 — Marca de agua, idea de Alberto 2026-07-18)** **ABM de configuración de marca de agua** (hoy es config global de appsettings: `Fotos:TextoWatermark/OpacidadWatermark/LadoMayorPreview/...`, ver ítem resuelto de Fase 0 más arriba — no hay pantalla para editarlo). Idea: permitir definir el texto/opacidad/"importancia" (agresividad de la grilla) **por tenant o por evento**, vía un ABM nuevo del vertical Fotos. Se conecta con una segunda idea — **posiblemente la MISMA funcionalidad, con un caso límite**: este aplicativo también lo podría usar el propio fotógrafo para mostrar fotos EN PERSONA (sin que medie una familia con su código) — ahí la marca de agua/fricción anti-copia no tendría sentido, porque no hay riesgo de fuga (es él mostrando desde su propio dispositivo). Propuesta a explorar: si quien mira es el ADMIN del tenant (el fotógrafo logueado), la app podría ofrecer un **modo visor simple sin la "seguridad" de familia** (sin watermark de overlay, sin fricción anti-copia, con más resolución). **Ojo, aclarar antes de tocar código**: esto NO es un agujero de seguridad nuevo — el admin YA puede ver el original limpio por `api/fotos/fotos/{id}/original` (ADR-06), así que técnicamente es una pantalla de PRESENTACIÓN que hoy no existe, no una excepción a ADR-01 (que habla de lo que ve la FAMILIA). Antes de implementar, decidir en un ADR: ¿la marca de agua pasa a tener niveles configurables (apagada/liviana/agresiva) por tenant o por evento?, ¿el "modo fotógrafo" es una pantalla nueva (ej. `/fotos/presentar`) o un comportamiento distinto de `/mi-album` cuando quien está logueado es el admin?
- [ ] **(Fase 3, feedback de Alberto 2026-07-18)** **Proceso de pago: falta terminar de definir el detalle**, más allá del ítem técnico del webhook (abajo). Al retomar Fase 3, repasar con Alberto el flujo completo (Checkout Pro vs. otras superficies de MP, qué pasa si el pago queda pendiente/rechazado, cómo se concilia con `MedioPago=Efectivo`, y el caso "seña vs. total" ya anotado más arriba en las preguntas respondidas) antes de empezar a codear.
- [ ] **(Fase 3)** Webhook de Mercado Pago necesita URL pública: en dev usar túnel (ngrok/cloudflared) o simulador.
- [ ] **(Fase 4)** Distribución de la app Capacitor: Play Store (USD 25 por única vez) vs APK por link directo. iOS requiere cuenta de developer (USD 99/año) — evaluar si se justifica o si iOS queda solo con la web. Mantener el Angular libre de dependencias solo-navegador para que el empaquetado no duela.
- [x] ~~**(Deploy)** **Migrar de SQL Server a PostgreSQL**~~ **RESUELTO (2026-07-24, ver ADR-14)**: `DatabaseFactory` corre sobre Npgsql (cutover directo, no dual), migraciones regeneradas, vista `vw_UsuarioRolesEfectivos` portada a mano, Respawn con `DbAdapter.Postgres`, sink `Serilog.Sinks.PostgreSQL`. Suite de integración 508/508 en verde contra Postgres local. Pendiente aparte (no bloquea esta migración): sembrar datos reales (root/tenant/fotógrafo) en la base de dev `AcgFotos`, hoy con el esquema aplicado pero vacía — ver docs/credenciales-dev.md.
- [ ] **(Deploy)** Elegir hosting y dominio. Backups automáticos de PostgreSQL. Política de retención de fotos tras `FechaExpiracion` (¿borrar originales o archivarlos? — preguntarle: quizá quiera conservarlos para reventa).
- [ ] **(Deploy, deuda técnica de la migración a Postgres, ADR-14)** El código heredado usa `DateTime.Now` (Kind=Local) en vez de `DateTime.UtcNow` en decenas de lugares — hoy tapado con el switch `Npgsql.EnableLegacyTimestampBehavior` (`DatabaseFactory`). Correcto a mediano plazo: auditar y migrar cada call site a UTC, después sacar el switch.

## Consideraciones legales / sensibles (no ignorar)

- [ ] Son **fotos de menores**: consentimiento (normalmente lo gestiona el colegio con las familias — confirmar cómo lo maneja hoy). No indexar nada públicamente (`robots.txt` + `noindex` + páginas solo accesibles con código).
- [ ] Datos personales mínimos: nombre y teléfono solo en el pedido; definir cuándo se borran.
- [ ] Si en el futuro hay multi-tenant (otros fotógrafos), revisar términos de responsabilidad sobre el contenido.

## Ideas descartadas (para no re-discutirlas)

- **DRM de video/imagen (EME/Widevine)** para ennegrecer capturas: complejidad absurda para fotos, comportamiento inconsistente por dispositivo, rompe UX, y la foto a la pantalla lo saltea igual. El bloqueo real de capturas va por la app Capacitor con `FLAG_SECURE` (ADR-01, capa 3).
- **Watermark manual por el fotógrafo** (Photoshop/Lightroom antes de subir): horas de trabajo por evento contra costo ≈ cero del sistema, y riesgo fatal de imprimir fotos con la marca. El original se sube limpio; el sistema marca solo los derivados.
- **Microservicios / serverless**: el volumen no lo justifica — ver ADR-04.
